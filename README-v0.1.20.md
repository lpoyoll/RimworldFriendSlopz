# Rimjob v0.1.20

Fixes the v0.1.19 shared starting-site regression caused by a Harmony return-type mismatch in the remote-pawn right-click patch.

- Restores same-tile first-settlement selection.
- Keeps strict remote pawn ownership.
- Adds a critical shared-tile Harmony bootstrap so future optional patch failures cannot silently remove the starting-site override.
- Carries forward v0.1.18 shared registration/session fixes, F9 diagnostics, Update.exe, Direct Connect, ZIP and MSI releases.
